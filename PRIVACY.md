# 隐私政策 · Privacy Policy

「子午流注 · 十二时辰经络图」浏览器扩展（下称「本扩展」）尊重并保护您的隐私。

## 数据收集

**本扩展不收集、不存储、不传输任何用户数据。**

- 不请求任何浏览器权限（无 `permissions` / `host_permissions`）
- 不包含任何分析、统计、广告或第三方追踪代码
- 不发起任何网络请求：页面内容、农历历法数据与字体均打包在扩展内本地运行
- 不使用 Cookie、localStorage 或其他持久化存储

## 数据使用

由于不收集任何数据，因此不存在数据使用、共享或出售行为。

## 变更

本政策如有变更，将在本页面更新并随扩展版本说明告知。

---

# Privacy Policy (English)

The "Ziwu Liuzhu · Twelve Shichen Meridian Map" extension ("this extension") does **not** collect, store, or transmit any user data.

- It requests no browser permissions (`permissions` / `host_permissions` are absent).
- It contains no analytics, advertising, or third-party tracking code.
- It makes no network requests: all content, calendar data, and fonts are bundled and run locally.
- It uses no cookies, localStorage, or any persistent storage.

Since no data is collected, no data is used, shared, or sold. Changes to this policy will be posted on this page.
